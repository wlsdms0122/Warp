//
//  Expression.swift
//  WarpDocument
//
//  Created by JSilver on 8/16/26.
//

import Warp

// Foundation declares an `Expression` of its own, so an unqualified mention in
// this target is ambiguous. The language's is the one meant, everywhere here.
public typealias Expression = Warp.Expression
